"""Vendored MetaGPT core for stanford-town-vue.

This package contains a minimal copy of the MetaGPT framework's core
modules (schema, context, team, role, action, memory, environment, LLM
providers, configs, utils, logs, const) — sufficient to run the
StanfordTown simulator without depending on the upstream `metagpt`
package. Other components of the upstream framework (rag, tools, ext,
strategy, learn, document_store, most other roles/actions/providers)
are intentionally NOT vendored.
"""
