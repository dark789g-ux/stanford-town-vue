# stanford-town-vue-backend

FastAPI backend for the Stanford Town Vue project. See the top-level [README](../README.md) and [docs/](../docs/) for the full project documentation.

## Install (editable)

```powershell
pip install -e .
alembic upgrade head
uvicorn app.main:app --reload
```
